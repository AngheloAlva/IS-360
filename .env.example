# ─── IS 360 Demo — variables de entorno ────────────────────────────────────────
# Esta demo corre 100% client-side: PGlite (Postgres WASM) + MSW interceptando
# las API routes en el browser. NO requiere DATABASE_URL ni credenciales reales.

# Activa el modo demo. Si no está, o si es "false", la app intenta hablar con
# Prisma/Postgres real (fallaría sin DATABASE_URL).
NEXT_PUBLIC_DEMO_MODE=true
